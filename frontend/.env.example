# Backend API base URL (no trailing slash)
EXPO_PUBLIC_API_URL=http://localhost:8000/api/v1

# Set to "true" to use mock data instead of the real API (when backend is not running)
EXPO_PUBLIC_USE_MOCK=false
